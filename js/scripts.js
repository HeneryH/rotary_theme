/**
 * @file
 * Behaviors for the Rotary_Theme theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.Rotary_Theme = {
    attach: function (context) {

      // Rotary_Theme JavaScript behaviors goes here.
    }
  };

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
